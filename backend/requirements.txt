# ── Ideogram 4.0 Local Studio ─ Python Requirements ─────────────────────────
# Install order matters — torch must come before ideogram4 and diffusers.
#
# PyTorch (CUDA 12.6 — adjust index URL if your driver needs a different version)
# torch>=2.12.0 wheels exist on cu126; cu124 index only has up to 2.11.x.
# If torch is already installed for your GPU, skip this line and remove --index-url
# from install.bat.
torch>=2.12.0

# Ideogram 4 official inference package (fp8 + nf4 support)
# Installed from source in install.bat: pip install git+https://github.com/ideogram-oss/ideogram4.git
# Listed here for reference only — do not pip install -r requirements.txt directly.
# ideogram-4   <-- installed from git in install.bat

# HuggingFace diffusers (main branch — Ideogram4Pipeline not yet in a release)
# Installed from source in install.bat.
# diffusers    <-- installed from git in install.bat

# transformers (dependency of both ideogram4 and diffusers)
# NOTE: kept at 4.x floor — ideogram4 and diffusers (git installs) were written
# against 4.49.x; transformers 5.x has breaking API changes. Bump only after
# verifying ideogram4 package compatibility.
transformers>=4.49.0
safetensors>=0.8.0
accelerate>=1.13.0
einops>=0.7.0
sentencepiece
pillow
huggingface_hub>=1.18.0
requests>=2.28
bitsandbytes>=0.49.2

# Web framework
fastapi>=0.137.0
uvicorn[standard]>=0.49.0
python-multipart>=0.0.9

# Database
aiosqlite>=0.22.1

# Settings / env
pydantic-settings>=2.14.1
python-dotenv>=1.2.2

# Post-generation upscaling
# spandrel: architecture-agnostic loader for ESRGAN / HAT / NomosUniDAT etc.
spandrel>=0.4.2
# aura-sr: GigaGAN-based upscaler — best quality for AI-generated images
# NOTE: PyPI latest is 0.0.4 (package stagnant); install from git for latest:
#   pip install git+https://github.com/fal-ai/aura-sr
aura-sr>=0.0.4
